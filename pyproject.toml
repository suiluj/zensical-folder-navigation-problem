[project]
name = "zensical-folder-navigation-problem"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "suiluj", email = "suiluj@users.noreply.github.com" }
]
requires-python = ">=3.11"
dependencies = [
    "zensical>=0.0.65",
]

[project.scripts]
zensical-folder-navigation-problem = "zensical_folder_navigation_problem:main"

[build-system]
requires = ["uv_build>=0.11.16,<0.12.0"]
build-backend = "uv_build"
